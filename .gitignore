# Private working memory for LLM sessions — never committed (see docs/ for the public design)
CLAUDE.md

# Secrets
.env
.env.*
!.env.example
frontend/.env
frontend/.env.*
!frontend/.env.example

# Derived working copy of the data (rebuilt from data/*.json by the jobs)
*.sqlite
*.sqlite3
*.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Node / frontend
node_modules/
frontend/dist/
frontend/dist-ssr/
.vite/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Generated data lives on the `data` branch (see scripts/data-branch.sh). Only the hand-edited
# files in data/ are on main, and the site bundle is rebuilt at deploy time.
data/hackathons/
data/ideas/
data/labels/
data/winners/
data/source_runs.json
data/source_health.json
frontend/public/data/

scratch/
